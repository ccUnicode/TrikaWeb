---
// 1. Definimos la prop que contendrá el nombre del examen
export interface Props {
  titulo: string;
}
const { titulo } = Astro.props;
---

<div class="mb-6 flex items-center justify-between">
  
  <h2 class="text-2xl font-semibold md:text-3xl">{titulo}</h2>
  
  <button class="hidden text-sm font-medium text-gray-300 border border-gray-700 rounded-lg px-3 py-1 hover:border-gray-500 hover:text-white md:block">
    Ordenar por ciclo v
  </button>
</div>