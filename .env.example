# ======== SERVER ONLY (NO se expone al navegador) ========
# Copiar a: .env

SUPABASE_URL=""                 # p.ej. https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=""         # service_role o secret key (NUNCA en el cliente)
IP_SALT=""                      # cadena aleatoria larga (para hash de IP)
ADMIN_PASS=""                   # contraseña para /admin (subidas, sync, etc.)

# (Opcional futuro)
# TURNSTILE_SECRET_KEY=""
# SENTRY_DSN=""

# ======== CLIENTE/NAVEGADOR (se expone) ========
# Copiar a: .env.local  (en Astro deben iniciar con PUBLIC_)

PUBLIC_SUPABASE_URL=""          # igual a SUPABASE_URL
PUBLIC_SUPABASE_ANON_KEY=""     # anon/publishable key

# (Opcional futuro)
# PUBLIC_TURNSTILE_SITE_KEY=""
# PUBLIC_APP_ENV="dev"
